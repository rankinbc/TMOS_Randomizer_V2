# harness outputs

Run artifacts from the `harness` component land here as `<YYYY-MM-DD>_<run-description>/` subfolders. Each subfolder holds `report.md`, `report.json`, and `candidate.json` for a single-seed run. Keep latest ~5; move older to `_archive/`.

# visualizer outputs

Run artifacts from the `visualizer` component land here as `<YYYY-MM-DD>_<run-description>/` subfolders. Contains PNG (and optional SVG for diffs) renderings of candidate maps, metric distribution plots, and diff visualizations. Keep latest ~5; move older to `_archive/`.

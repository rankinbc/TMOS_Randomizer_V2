# benchmark outputs

Run artifacts from the `benchmark` component land here as `<YYYY-MM-DD>_<run-description>/` subfolders. Each subfolder holds `summary.md`, `summary.json`, and `per_seed.ndjson` for one N-seed sweep. Keep latest ~5; move older to `_archive/`.

# snapshots inputs

Pre-parsed JSON snapshots of the ROM (cached artifact per REQUIREMENTS.md §8 Q3 — decouples the Lab from V2 parser churn and speeds up cold starts). Consumed by: harness, benchmark.

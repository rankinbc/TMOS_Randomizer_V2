# notebooks

Exploratory hypothesis-testing Jupyter notebooks outside the main pipeline. Keep ad-hoc; promote anything durable into `src/tmos_strategy_lab/` or a component.

Per REQUIREMENTS.md §8 Q6, notebooks are committed to git with outputs stripped — install `nbstripout` as a pre-commit hook: `pip install nbstripout && nbstripout --install`.

# rom inputs

Drop ROM files here. Consumed by: `world_editor`, `randomization_lab`.

Canonical file: `TMOS_ORIGINAL.nes` (MD5 `b3236db14c87f375e5f24a5b9b79f071`). Not tracked in git (`.gitignore` excludes `*.nes`).
